{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <div class="d-flex flex-justify-content-center">
        <h1 class="display-1 border border-dark rounded mx-auto mt-2 p-2">User</h1>
    </div>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ user.login }}</td>
            </tr>
            <tr>
                <th>Rôles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Mot de passe</th>
                <td>{{ user.password }}</td>
            </tr>
        </tbody>
    </table>
<<<<<<< HEAD
    
    <div class="d-flex flex-row-reverse px-2 justify-content-space-around">
        {% if is_granted('ROLE_ADMIN') %}
        {{ include('user/_delete_form.html.twig') }}
        <a class="btn btn-primary mx-2" href="{{ path('app_user_edit', {'login': user.login}) }}">edit</a>
        {% endif %}
        <a class="btn btn-secondary" href="{{ path('app_user_index') }}">back to list</a>
    </div>
    
=======

    <a href="{{ path('app_user_index') }}">Retour à la liste</a>

    <a href="{{ path('app_user_edit', {'login': user.login}) }}">Modifier</a>

    {{ include('user/_delete_form.html.twig') }}
>>>>>>> refs/remotes/origin/CRUD
{% endblock %}

<form method="post" action="{{ path('app_user_delete', {'login': user.login}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.login) }}">
    <button class="btn">Supprimer</button>
</form>
